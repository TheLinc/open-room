blank_issues_enabled: true
contact_links:
  - name: Claude Code itself
    url: https://github.com/anthropics/claude-code/issues
    about: Problems with signing in, the CLI, or the model's behaviour belong upstream. Open Room only drives the CLI.
